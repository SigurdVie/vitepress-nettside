{
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "scripts": {
    "docs:dev": "node getArticles.mjs && vitepress dev docs",
    "docs:build": "node getArticles.mjs && vitepress build docs",
    "docs:preview": "node getArticles.mjs && vitepress preview docs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "remove-markdown": "^0.6.0"
  }
}
